<Window x:Class="TheHastyFinn.XFactorUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TheHastyFinn"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="XFactorUI" Height="640" Width="800">
    <Window.DataContext>
        <local:XFactorGraphModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <oxy:PlotView Model="{Binding TickerModel}" Grid.Row="1"/>
        <oxy:PlotView Model="{Binding XFactorModel}" Grid.Row="2"/>
    </Grid>
</Window>
